# Dev/test toolchain — single source of truth for the project Python env.
#   uv venv --python 3.12 && uv pip install -r requirements-dev.txt
ansible-core==2.16.19
ansible-lint==26.6.0
yamllint==1.38.0
molecule==25.1.0
molecule-plugins==23.7.0
python-vagrant==1.0.0
pywinrm==0.4.3
pre-commit>=3.5,<4
